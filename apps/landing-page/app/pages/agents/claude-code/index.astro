---
/*
 * /agents/claude-code/ — agent detail page.
 *
 * Captures "claude code for design", "claude code design" intent and
 * drives to Open Design as the missing design layer. Linked from the
 * `/agents/` hub. All copy and resource links come from
 * `copy.agentGuides['claude-code']`; never fabricate URLs here.
 */
import Layout from '../../../_components/sub-page-layout.astro';
import { getInfoPageCopy } from '../../../info-page-i18n';
import { localeFromPath, localizedHref } from '../../../i18n';

const SLUG = 'claude-code';
const SITE = Astro.site?.toString() ?? 'https://open-design.ai/';
const REPO = 'https://github.com/nexu-io/open-design';
const REPO_RELEASES = `${REPO}/releases`;
const locale = localeFromPath(Astro.url.pathname);
const href = (path: string) => localizedHref(path, locale);
const copy = getInfoPageCopy(locale);
const common = copy.common;
// Compact locales fall back to the English guide rather than crash on undefined.
const page = copy.agentGuides[SLUG] ?? getInfoPageCopy('en').agentGuides[SLUG]!;

const jsonLd = [
  {
    '@context': 'https://schema.org',
    '@type': 'BreadcrumbList',
    itemListElement: [
      { '@type': 'ListItem', position: 1, name: 'Open Design', item: SITE },
      { '@type': 'ListItem', position: 2, name: 'Agents', item: `${SITE}agents/` },
      { '@type': 'ListItem', position: 3, name: page.breadcrumb, item: `${SITE}agents/${SLUG}/` },
    ],
  },
  {
    '@context': 'https://schema.org',
    '@type': 'WebPage',
    name: page.title,
    description: page.description,
    url: `${SITE}agents/${SLUG}/`,
    isPartOf: { '@id': `${SITE}#website` },
  },
  {
    '@context': 'https://schema.org',
    '@type': 'FAQPage',
    mainEntity: page.faq.map(({ name, text }) => ({
      '@type': 'Question',
      name,
      acceptedAnswer: { '@type': 'Answer', text },
    })),
  },
];
---

<Layout title={page.title} description={page.description} active="home" jsonLd={jsonLd}>
  <nav class="breadcrumb" aria-label={common.breadcrumbAria}>
    <a href={href('/')}>Open Design</a>
    <span>/</span>
    <a href={href('/agents/')}>Agents</a>
    <span>/</span>
    <span aria-current="page">{page.breadcrumb}</span>
  </nav>

  <article class="info-page">
    <header class="catalog-head">
      <span class="label">{page.label}</span>
      <h1 class="display">{page.heading}</h1>
      <p class="lead">{page.lead}</p>
    </header>

    <div class="tldr-card">
      <h3>{page.tldrTitle}</h3>
      <p>{page.tldrBody}</p>
    </div>

    <nav class="info-toc" aria-label={common.onThisPage}>
      <span>{common.onThisPage}</span>
      <a href="#about">{page.toc[0]}</a>
      <a href="#design">{page.toc[1]}</a>
      <a href="#resources">{page.toc[2]}</a>
      <a href="#with-od">{page.toc[3]}</a>
      <a href="#faq">{page.toc[4]}</a>
    </nav>

    <section class="info-section" id="about">
      <h2>{page.aboutTitle}</h2>
      {page.aboutBody.map((paragraph) => (
        <p>{paragraph}</p>
      ))}
      <ul>
        <li><strong>{page.vendorLabel}:</strong> {page.vendor}</li>
        <li><strong>{page.credentialLabel}:</strong> {page.credential}</li>
      </ul>
    </section>

    <section class="info-section" id="design">
      <h2>{page.designTitle}</h2>
      <p>{page.designLead}</p>
      <ol>
        {page.designPoints.map((item) => (
          <li><strong>{item.label}</strong> {item.body}</li>
        ))}
      </ol>
    </section>

    <section class="info-section" id="resources">
      <h2>{page.linksTitle}</h2>
      <p>{page.linksLead}</p>
      <ul class="resource-list">
        {page.links.map((l) => (
          <li>
            <a href={l.href} target="_blank" rel="noreferrer noopener">{l.label}</a>
            <span class="resource-source">{l.source}</span>
          </li>
        ))}
      </ul>
    </section>

    <section class="info-section" id="with-od">
      <h2>{page.withOdTitle}</h2>
      <p>{page.withOdLead}</p>
      <ol>
        {page.withOdSteps.map((step) => (
          <li>{step}</li>
        ))}
      </ol>
      <p>{page.withOdClosing}</p>
    </section>

    <section class="info-section" id="faq">
      <h2>{page.faqTitle}</h2>
      <ol class="faq-list">
        {page.faq.map(({ name, text }, idx) => (
          <li class="faq-item">
            <details>
              <summary>
                <span class="faq-index">{String(idx + 1).padStart(2, '0')}</span>
                <span class="faq-q">{name}</span>
                <span class="faq-toggle" aria-hidden="true">+</span>
              </summary>
              <p class="faq-a">{text}</p>
            </details>
          </li>
        ))}
      </ol>
    </section>

    <section class="info-cta" aria-label="Open Design call to action">
      <div>
        <h2>{page.ctaTitle}</h2>
        <p>{page.ctaBody}</p>
      </div>
      <div class="info-cta-actions">
        <a class="btn btn-primary" href={REPO} target="_blank" rel="noreferrer noopener">{common.starOnGithub}</a>
        <a class="btn btn-ghost" href={href('/quickstart/')}>{common.quickstart}</a>
        <a class="btn btn-ghost" href={REPO_RELEASES} target="_blank" rel="noreferrer noopener">{common.downloadDesktop}</a>
      </div>
      <div class="info-cta-meta">
        <span class="stamp">● {common.apache}</span>
        <span>{common.localFirst} · {common.byok}</span>
        <span><a class="inline-link" href={href('/agents/')}>{copy.agents.breadcrumb}</a></span>
      </div>
    </section>
  </article>
</Layout>
